# WebRTC 単方向配信システム 要件定義

## 1. 目的
- スマートフォン2台を使い、送信者1名 → 受信者1名への単方向動画配信を行う。
- ブラウザで動作するWebページと、同一ルーム制御用シグナリングサーバーを提供する。

## 2. スコープ
- **対象**
  - WebRTC P2P による映像配信（音声なし）
  - 文字列ルームIDでの入室
  - Node.js製シグナリングサーバー
  - Vite + React クライアント
  - Dockerコンテナ化
- **非対象**
  - SFU/MCU
  - TURNサーバー
  - 認証・認可
  - 録画・アーカイブ

## 3. 想定利用条件
- 同一Wi-Fiネットワーク内での利用のみ（NAT越えは考慮外）。
- 受信者は各ルームにつき最大1名。
- 送信者はカメラ選択時にインカメ/アウトカメ相当を選べる。

## 4. 機能要件
### 4.1 ルーム
- ユーザーは任意文字列のルームIDを入力できる。
- ロールは `sender` / `receiver` を選択可能。
- 同一ルームへの入室は以下制約:
  - sender: 1名まで
  - receiver: 1名まで
- 定員超過時はエラーメッセージを表示し入室不可。

### 4.2 シグナリング
- WebSocketでクライアントとサーバーを接続。
- メッセージ種別:
  - `join`
  - `joined`
  - `peer-ready`
  - `offer`
  - `answer`
  - `ice-candidate`
  - `room-error`
  - `peer-left`
- SDP/ICEは同一ルーム内の相手1名へ中継。

### 4.3 映像
- sender は `getUserMedia({ video, audio: false })` を使用。
- sender はカメラ向き (`user` / `environment`) を選択可能。
- receiver は受信映像を `<video autoplay playsInline>` で表示。
- 音声トラックは送信しない。

### 4.4 接続制御
- sender が先/後どちらで入室しても確立できる。
- 切断時は相手に `peer-left` 通知。
- 再入室で再接続可能。

## 5. 非機能要件
- モバイルChrome/Safariの最新安定版を主対象とする。
- HTTPSまたはlocalhost相当でカメラ利用可能な実行形態を想定。
- エラーメッセージは利用者が復旧行動を取れる文言にする。

## 6. UI要件
- 1ページ内で以下を提供:
  - ルームID入力
  - ロール選択
  - カメラ向き選択（sender時のみ）
  - 参加/退出ボタン
  - ログ表示
  - ローカル映像（sender）
  - リモート映像（receiver）

## 7. アーキテクチャ要件
- フロントエンド: Vite + React
- サーバー: Node.js + Express + ws
- WebSocketエンドポイント: `/ws`
- フロント配信とシグナリングを同一Nodeプロセスで提供

## 8. データ/プロトコル要件
- JSONメッセージフォーマット:
```json
{ "type": "offer", "roomId": "abc", "payload": { "sdp": "..." } }
```
- `roomId` は1〜64文字の英数字/`-`/`_` 推奨（厳密制限は実装側で検証）。

## 9. コンテナ要件
- マルチステージビルドで client をビルドし、server が静的配信。
- 公開ポート: `5173`
- コンテナ起動で即利用可能。

## 10. 受け入れ基準
- 2台のスマホで同一ルームIDに sender/receiver として参加できる。
- receiver 側で sender の映像が表示される。
- 音声が送信されない。
- 2人目receiverの入室は拒否される。
- sender がインカメ/アウトカメを切り替え可能。
